"""
run.py — Entry point for ResolveX-AI backend server.
Starts the FastAPI app using uvicorn.
"""

import uvicorn

if __name__ == "__main__":
    uvicorn.run(
        "app.main:app",
        host="0.0.0.0",
        port=8000,
        reload=True,
        log_level="info",
    )
